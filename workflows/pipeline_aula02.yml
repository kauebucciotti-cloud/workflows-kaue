name : Pipeline_Aula 2

on: 
  push: 
    branches:
      - main

jobs: 

  build:
    runs-on: unbutu-letest

    steps:
      - name:  Checkout codigo
        uses: action/checkout@v3
      
      - name: Comfigurar Python
        uses: actions/stup-python@v4
        whith: 
          python-version:  '3.11'

      - name: instalar dependencias
        run: |
          python -m pip install --upgrade pip install -r requirements.txt 

      - name: Rodar testes
        run: |
          pytest

  